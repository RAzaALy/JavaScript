// There are three ways to convert the string in to numbers.

// unry Plus Operator

var n = +str;

// The Number Constructor 
var n = Number(str);

// the parseFloat function 
var n = parseFloat(str);
